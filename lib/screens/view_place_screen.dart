import 'package:flutter/material.dart';

class ViewPlaceScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Place details'),
      ),
      body: Center(
        child: Text('View place'),
      ),
    );
  }
}
